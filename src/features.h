#ifndef FEATURES_H
#define FEATURES_H


void helloWorld();
void dimension (const char *source_path);
void second_line(const char *source_path);
void first_pixel(char *source_path);
void print_pixel(const char *filename, int x, int y );
void stat_report(const char* image_path);

#endif
